---
- name: Wordpress service
  ansible.builtin.include_role:
    name: "{{ roles.role }}"
  loop_control:
    loop_var: roles
  loop:
    - role: cloudwm.ansible-common-role
    - role: cloudwm.ansible-sendmail-role
    - role: cloudwm.ansible-phpmyadmin-role
    - role: cloudwm.ansible-certbot-role
    - role: cloudwm.ansible-nginx-enable-letsencrypt-role
    - role: cloudwm.ansible-wordpress-role
